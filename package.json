{
  "name": "gpl-virtual-stand",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:client": "vite",
    "dev:server": "python -m uvicorn backend.app:app --reload --port 8000",
    "build": "vite build",
    "preview": "vite preview",
    "start:server": "python -m uvicorn backend.app:app --port 8000"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
